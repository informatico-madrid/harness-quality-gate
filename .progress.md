## Completed Tasks
- [x] 1.2 Create `pyproject.toml` with package metadata + pytest config   - feat(build): add pyproject.toml with v2.0.0 package metadata
- [x] 1.5 Implement detection cache + fingerprint file   - feat(detector): add detection cache with mtime + git-HEAD invalidation

## Completed Tasks
- [x] 1.2 Create `pyproject.toml` with package metadata + pytest config   - feat(build): add pyproject.toml with v2.0.0 package metadata
- [x] 1.5 Implement detection cache + fingerprint file   - feat(detector): add detection cache with mtime + git-HEAD invalidation
- [x] 1.14 Implement `composer_audit_adapter.py`   - feat(adapters/php): composer-audit adapter preserving CVE/CWE
- [x] 1.16 Implement `php_adapter.py` orchestrator (L3A wiring only)   - feat(adapters/php): php_adapter orchestrator wiring L3A
- [x] 1.28 Implement `config.py` v2 loader + v1 hard-rejection   - feat(config): v2 loader with v1 hard-rejection and ramp policy

## Current Task
Awaiting next task

## Learnings
- `pip install -e .` requires `[build-system]` with setuptools>=61.0 for PEP 621 pyproject.toml support; `--break-system-packages` flag needed on this system due to PEP 668 restriction.
- Cache stores full Detection dataclass via dataclasses.asdict() in JSON; fingerprint uses git rev-parse HEAD when in a git repo, otherwise writes `<no-git>` sentinel.
## Learnings
- PHP CS Fixer JSON output supports both simple (name+diff) and detailed (violations array) formats; parse() handles both.

## Completed Tasks
- [x] 1.34 Delete relocated scripts/ files - chore: delete relocated scripts/ files
- [x] 1.35 Clean up scripts/__pycache__ and update .gitignore - chore: clean up scripts/__pycache__ and update .gitignore

## Learnings
- llm_solid_judge.py, antipattern_judge.py, diversity_metric.py were relocated from scripts/ to bmad/ in prior refactor commits (0bc5b30, c5d3fef, facf00a); cleanup tasks verified files already absent from scripts/.
- .gitignore already covered __pycache__/, *.py[cod], .mypy_cache/, .pytest_cache/, htmlcov/; only _quality-gate/ was missing and has been added.
